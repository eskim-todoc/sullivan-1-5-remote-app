---
name: 작업 목록 루트 인덱스
purpose: 모듈별 작업 목록으로 가는 진입점
type: 인덱스
tags: [작업목록, 인덱스]
---

# 작업 목록

**TL;DR**: 이 저장소의 작업은 모듈별 폴더 아래에 `YYYYMMDD_HHmm_<주제>` 로 쌓인다. 모듈 목록은 아래 표에 있고, 각 모듈의 작업 상태는 그 모듈의 작업 목록에서 본다.

| 모듈 | 작업 목록 |
|---|---|
| UI (화면·레이아웃) | [UI/작업 목록.md](UI/작업%20목록.md) |
| 프로토콜 (0x59) | [프로토콜/작업 목록.md](프로토콜/작업%20목록.md) |
