---
name: 글래스모피즘 테마 이력 및 결과
purpose: 리모콘 앱 전체 디자인을 글래스모피즘으로 바꾸는 작업의 시계열 기록과 완료 요약
type: 이력
tags: [리모콘앱, UI, 디자인, 글래스모피즘, 테마]
---

# 이력 및 결과

**TL;DR**: 앱 디자인을 글래스모피즘 테마로 바꾼다. 은수님이 `ui-glass` 에이전트 활용을 지정하셨다. **최대 난점은 `minSdk 23`** — 실시간 블러(`RenderEffect`)가 API 31+ 전용이라 하위 구간 폴백이 설계의 중심이 된다. 의료기기 리모컨이라 **가독성이 안전 요건**이다.

## 시계열

| 시각 | 단계 | 내용 |
|---|---|---|
| 2026-09-07 15:17 | ① 착수 | 은수님 지시 — *"ui-glass 에이전트와 함께 지금 앱 디자인을 글래스모피즘 테마로 변경해봐"* |
| 15:17 | ① 실사 | 자산 파악 완료. 화면 18개 · drawable 47개 · 테마는 `styles.xml` 단일 파일(`themes.xml` 없음) · 팔레트 10단 램프 4계열 |
| 15:17 | ① 제약 확정 | **`minSdk 23`** 확인 (`app/build.gradle:10`). `RenderEffect.createBlurEffect` 는 API 31+ |
| 15:17 | ① 모듈 배치 | `디자인/` 을 새로 만들었다가 **철회** — 루트 인덱스에 없는 모듈이라 기존 `UI` 아래로 옮김 |
| 15:17 | ① 위임 | `ui-glass` 에이전트에 읽기 전용 실사·설계 위임 (게이트 1 전이므로 쓰기 금지, 산출물은 로그 파일만) |
| 15:24 | ① 인터뷰 | 범위·밝기·구형기기·선행작업 4문 질의 → **넷 다 권장안 채택** (리모컨 화면 시범 · 다크 글래스 · 기기 이원화 · 4.5 먼저 커밋) |
| 15:24 | 선행 정리 | 4.5 프로토콜 작업을 **`b55a663`** 로 커밋. 단말 시리얼(`deploymentTargetSelector.xml`) 제외, AI 라인 없음 확인. 빌드 통과 후 커밋 |
| 15:24 | ① 완료 | `요구사항.md` 작성 — 요구사항 7건 · 수용 기준 6건. **요구사항_4(가독성 우선)를 양보 불가로 못박음** |
| 15:31 | **정정** | 내가 «본문은 Light» 로 잘못 전제했다. 실제로는 `fragment_remote_control.xml:8` 이 `white_10`(**`#191919`**)을 깔아 **이미 어둡다**. `white_*` 램프는 이름이 뒤집혀 있다. 에이전트에도 정정 전달 |
| 15:31 | 계측 | 유리 두께 예산을 직접 계산. **경고 빨강 `#FF5252` 가 병목** — 색 유지 시 흰빛 상한 **7%**. 두꺼운 유리를 원하면 경고색을 밝혀야 한다 |
| 15:31 | 발견 | 분류 색이 **레이아웃 10곳 + Java 상수 4개**에 중복. 한쪽만 고치면 경고 해제 시 복원색이 어긋난다 |
| 15:31 | 발견 | Java 가 색을 지정하는 곳은 앱 전체 **3곳뿐** — 테마 작업은 XML 중심으로 간다 |
| 15:52 | ② 에이전트 회신 | `ui-glass` 설계 도착 — 201줄. 배경 View 분리·블롭·위험 6건·단계 3안 |
| 15:55 | ② **검산** | 파일 근거 8건 **전부 사실**. 다만 **블롭 합성 수치가 일관되게 낙관적**(제조번호 주장 3.78 → 실제 **3.18**, ALERT 2.31 → **1.99**). 평면 계산·배지 값은 정확히 일치 |
| 15:58 | ② **전제 뒤집기** | 에이전트는 유리를 «밝히는 판»으로만 봤다. **어둡히는 판**으로 바꾸면 컨테이너 7개 전부를 유리로 하면서 **색을 하나도 안 바꿔도** AA 를 지킨다(최악 5.23:1). 에이전트 원안의 제조번호 미달(3.18)도 **7.06 으로 해소** |
| 16:00 | ② 완료 | `계획.md` 작성 — 결정 4건 · Step 7개 · 실패 시나리오 4건. **게이트 1 대기** |
| 2026-09-08 07:44 | **게이트 1 통과** | 은수님이 넷 다 권장안 채택 — 검은 유리 · 55% · 실블러 없음 · 기존 색 재사용 |
| 07:44 | ③ 검증리스트 | Claude 열 13항 · 은수님 열 9항으로 산출 |
| 07:55 | ④ 구현 | drawable 2개 신규 + 레이아웃 컨테이너 8곳 + `dimens.xml` 2개. **Java 변경 0줄** |
| 07:55 | 벗어남_1 | 배경을 별도 View 가 아니라 **루트 배경**으로 넣음. 루트 패딩 때문에 자식 View 는 음의 마진이 필요한데 그 값이 `tdc_screen_padding_*` 과 두 곳에 살게 된다. 블러를 안 넣기로 해 View 를 뗄 이유가 사라졌다 |
| 07:55 | 벗어남_2 | 검색 오버레이 제외 — 카드가 아니라 **화면 전체 차단막**(`clickable=true`·`alpha=0.9`)이다 |
| 07:55 | ④ 자체검증 | **빌드 통과**. 최악 대비 **4.72:1**(AA) · 테두리 3.72:1 · id 90개 그대로 · Java·`colors.xml`·`styles.xml`·다른 화면 변경 0건 |
| 07:55 | **게이트 2 대기** | 은수님 열 9항은 실기가 필요하다 |
| 16:00 | 별건 | `build.gradle:72` 의 `lifecycle-livedata-ktx` 버전에 **`$` 가 빠짐**. Gradle 이 `lifecycle_version -> 2.9.1` 로 우연히 구제해 빌드가 통과할 뿐 |

## 확정된 사실

| 항목 | 값 | 근거 |
|---|---|---|
| `minSdk` | **23** (Android 6.0) | `source/app/build.gradle:10` |
| `targetSdk` / `compileSdk` | 35 / 34·35 혼재 | `build.gradle:11` · `:6`·`:39` — **`compileSdk` 가 두 번 선언돼 있다** |
| UI 시스템 | XML View + DataBinding·ViewBinding | `build.gradle:25~26` |
| 테마 부모 | `Theme.MaterialComponents.Light.NoActionBar` | `res/values/styles.xml:4` |
| 모서리 | `cornerFamily: cut` · 2dp (둥글지 않음) | `styles.xml` ShapeAppearance 2종 |
| 폰트 | 나눔고딕 | `styles.xml:10` |
| 화면 수 | 레이아웃 18개 | `res/layout/` |
| 태블릿 대응 | `sw600dp`·`sw720dp`·`sw900dp` 기존재 | `res/` |

## 발견 — 이번 작업과 별개의 흠

**`compileSdk` 가 `build.gradle` 에 두 번 선언돼 있다** (`:6` 에 35, `:39` 에 34). 뒤가 이긴다. 이번 작업의 원인은 아니나 고칠 값어치가 있어 남긴다.

## 완료 요약

(작업 완료 시 채운다)
